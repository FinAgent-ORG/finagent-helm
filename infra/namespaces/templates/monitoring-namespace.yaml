apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespaces.monitoring }}
  labels:
    {{- include "finagent-namespaces.labels" . | nindent 4 }}
    finagent.io/scope: monitoring
    shared-gateway: "true"
